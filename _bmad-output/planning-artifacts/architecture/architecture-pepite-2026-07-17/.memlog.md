---
scope: Back-office CMS pour Cadeau Malin
purpose: Build substrate pour bmad-create-epics-and-stories + bmad-dev-story
altitude: feature
updated: 2026-07-17T16:32
---

- (decision) AD-1 Paradigme: file d'attente Supabase relayee par agent. Le back-office (client statique) n'ecrit JAMAIS directement dans git. Il insere une intention dans une table Supabase (manual_content_edits). Un relais cote VPS (reutilisant scripts/github_commit_changes.js + github_open_pr_or_push.js, deja utilises par Pepite) applique la modification et publie. Empeche: deux implementations independantes du chemin de publication qui divergeraient (conventions de commit differentes, gestion d'erreur differente).
- (decision) AD-2 Frontiere stricte: le back-office ne parle qu'a Supabase (Auth + table manual_content_edits) et, en lecture seule, aux fichiers publics du site (fetch direct des pages publiees pour afficher le contenu actuel). Il ne parle jamais a l'API GitHub ni n'invoque git. Empeche: un deuxieme chemin de publication qui contournerait le relais unique.
- (decision) AD-3 Proprietaire unique de l'ecriture fichier+git: scripts/github_commit_changes.js + scripts/github_open_pr_or_push.js, deja utilises par Pepite. Le relais du back-office DOIT reutiliser ces memes scripts (pas de logique git dupliquee). Empeche: deux conventions de commit/push incompatibles (l'une pour Pepite, l'autre pour le back-office).
- (decision) AD-4 Detection de conflit (FR-6): le back-office capture un hash (sha256) du contenu du fichier au moment du chargement de l'editeur (base_content_hash). Le relais, avant d'appliquer, recalcule le hash du fichier reel dans le repo ; mismatch => status=conflict, aucune ecriture, notification a l'utilisateur. Empeche: ecrasement silencieux d'une modification faite par Pepite entre le chargement et la publication.
- (decision) AD-5 Cadence du relais decouplee du heartbeat marketing: le heartbeat existant (CronCreate, ~3h) sert la creation de contenu strategique, PAS la reactivite attendue par SM-1 du PRD (moins de 5 minutes bout-en-bout). Un second job CronCreate dedie, cadence courte (ex. 1-2 min), verifie uniquement la table manual_content_edits et applique les entrees pending. Empeche: confondre les deux boucles et degrader soit la reactivite du back-office soit la cadence strategique du heartbeat marketing.
- (decision) AD-6 Auth/RLS: reutilisation exacte du pattern dashboard.html (Supabase Auth Google Sign-In + RLS restreinte a authenticated + email exact du proprietaire). Table manual_content_edits: policies insert+select restreintes a authenticated+owner email (jamais anon) - evite le blocage classificateur deja rencontre sur des policies anon.
- (decision) AD-7 Lecture directe, ecriture via file d'attente: le back-office lit le contenu actuel d'une fiche en fetchant directement la page publiee (deja publique, pas besoin de Supabase pour la lecture). Seule l'ecriture/publication passe par la file d'attente Supabase (AD-1).
- (constraint) Deploiement: site/admin.html livre dans le meme deploiement GitHub Pages que le reste du site (pas de nouvel environnement), meta robots noindex comme dashboard.html. Relais VPS: reutilise la cle SSH de deploiement deja en place, aucune nouvelle surface d'authentification.
- (event) Lint spine: 0 findings. Adversarial self-review (2 finalize_reviewers lenses): version-verification OK (zero nouvelle dependance, rien a verifier sur le web). Attaque adversariale a trouve 2 trous reels: (1) format de file_path non precise (relatif repo vs relatif site/), (2) moment exact du statut applied (commit local vs push reussi) non precise - les deux corriges immediatement dans le spine (AD-3 + Consistency Conventions).
- (event) spine finalized
